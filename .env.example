# Copia este archivo como `.env` en el servidor y completá los valores reales.
# `.env` está excluido del control de versiones: nunca lo subas al repositorio.

DB_DSN=mysql:host=localhost;port=3306;dbname=registro_personal;charset=utf8mb4
DB_USER=cambia_esto
DB_PASSWORD=cambia_esto
APP_BASE_PATH=/registro_gatos
CORS_ORIGIN=